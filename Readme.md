Pure `python 3` script to generate GNOME dynamic backgrouds from a series of images.

Usage:
```
python3 install.py IMAGES_FOLDER BACKGROUND_NAME
```

For more options:
```
python3 install.py -h
```


